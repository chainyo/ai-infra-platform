---
# The subject names below are placeholders. Replace them in an overlay or
# private fork once your identity provider maps real users into Kubernetes
# groups.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: platform-reader
subjects:
  - kind: Group
    apiGroup: rbac.authorization.k8s.io
    name: platform-readers
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: platform-reader
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: platform-operator
subjects:
  - kind: Group
    apiGroup: rbac.authorization.k8s.io
    name: platform-operators
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: platform-operator
