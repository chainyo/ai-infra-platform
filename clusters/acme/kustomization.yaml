---
# kustomization.yaml — acme example cluster declarations.
#
# This is an example public cluster baseline that demonstrates the shared
# ai-infra-platform module surface. Private overlays or client-specific
# repositories can extend it without changing the public baseline.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../platform/gitops
  - ../../platform/networking
  - ../../platform/observability
  - ../../platform/security
  - ../../platform/storage
